# اسلاید ارائه

فایل `index.html` ارائهٔ دفاع است. با کلیدهای جهت، فاصله و `F` تمام‌صفحه اجرا می‌شود. `N` یادداشت گوینده را نشان می‌دهد.

از ریشهٔ مخزن:

```bash
python3 -m http.server 8787
```

سپس `slides/index.html` را باز کنید.
